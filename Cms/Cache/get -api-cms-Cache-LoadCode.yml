info:
  name: get /api/cms/Cache/LoadCode
  type: http
  seq: 1
  tags:
    - Cache

http:
  method: GET
  url: "{{cmsUrl}}/api/cms/Cache/LoadCode"
  auth:
    type: bearer
    token: "{{bearerToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/cms/Cache/LoadCode"
      method: GET
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
