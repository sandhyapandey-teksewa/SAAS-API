info:
  name: get /api/cms/Cache/LoadDomainCode
  type: http
  seq: 2
  tags:
    - Cache

http:
  method: GET
  url: "{{cmsUrl}}/api/cms/Cache/LoadDomainCode"
  params:
    - name: domainId
      value: ""
      type: query
      disabled: true
  auth:
    type: bearer
    token: "{{bearerToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/cms/Cache/LoadDomainCode"
      method: GET
      params:
        - name: domainId
          value: ""
          type: query
          disabled: true
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
