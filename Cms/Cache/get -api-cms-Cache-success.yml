info:
  name: get /api/cms/Cache/success
  type: http
  seq: 3
  tags:
    - Cache

http:
  method: GET
  url: "{{cmsUrl}}/api/cms/Cache/success"
  params:
    - name: message
      value: ""
      type: query
      disabled: true
  auth:
    type: bearer
    token: "{{bearerToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/cms/Cache/success"
      method: GET
      params:
        - name: message
          value: ""
          type: query
          disabled: true
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
