info:
  name: get /api/cms/CmsBase/success
  type: http
  seq: 1
  tags:
    - CmsBase

http:
  method: GET
  url: "{{cmsUrl}}/api/cms/CmsBase/success"
  params:
    - name: message
      value: ""
      type: query
      disabled: true
  auth:
    type: bearer
    token: "{{bearerToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/cms/CmsBase/success"
      method: GET
      params:
        - name: message
          value: ""
          type: query
          disabled: true
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
