info:
  name: get /api/cms/Construction/GetActivityTypes
  type: http
  seq: 28
  tags:
    - Construction

http:
  method: GET
  url: "{{cmsUrl}}/api/cms/Construction/GetActivityTypes"
  auth:
    type: bearer
    token: "{{bearerToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/cms/Construction/GetActivityTypes"
      method: GET
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
