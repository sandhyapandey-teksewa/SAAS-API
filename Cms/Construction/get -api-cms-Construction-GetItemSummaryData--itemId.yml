info:
  name: get /api/cms/Construction/GetItemSummaryData/:itemId
  type: http
  seq: 34
  tags:
    - Construction

http:
  method: GET
  url: "{{cmsUrl}}/api/cms/Construction/GetItemSummaryData/:itemId"
  params:
    - name: itemId
      value: "5473956"
      type: path
  auth:
    type: bearer
    token: "{{bearerToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/cms/Construction/GetItemSummaryData/:itemId"
      method: GET
      params:
        - name: itemId
          value: ""
          type: path
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
