info:
  name: get /api/cms/Construction/GetPayEstimateGrid/:ebContractId
  type: http
  seq: 13
  tags:
    - Construction

http:
  method: GET
  url: "{{cmsUrl}}/api/cms/Construction/GetPayEstimateGrid/:ebContractId"
  params:
    - name: ebContractId
      value: "937760910"
      type: path
  auth:
    type: bearer
    token: "{{bearerToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/cms/Construction/GetPayEstimateGrid/:ebContractId"
      method: GET
      params:
        - name: ebContractId
          value: ""
          type: path
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
