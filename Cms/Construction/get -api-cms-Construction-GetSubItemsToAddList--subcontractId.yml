info:
  name: get /api/cms/Construction/GetSubItemsToAddList/:subcontractId
  type: http
  seq: 37
  tags:
    - Construction

http:
  method: GET
  url: "{{cmsUrl}}/api/cms/Construction/GetSubItemsToAddList/:subcontractId"
  params:
    - name: subcontractId
      value: "378265"
      type: path
  auth:
    type: bearer
    token: "{{bearerToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/cms/Construction/GetSubItemsToAddList/:subcontractId"
      method: GET
      params:
        - name: subcontractId
          value: ""
          type: path
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
