info:
  name: get /api/cms/Construction/GetUnitOfMeasurement
  type: http
  seq: 66
  tags:
    - Construction

http:
  method: GET
  url: "{{cmsUrl}}/api/cms/Construction/GetUnitOfMeasurement"
  auth:
    type: bearer
    token: "{{bearerToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/cms/Construction/GetUnitOfMeasurement"
      method: GET
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
