info:
  name: get /api/cms/Construction/ReportServiceItemEntry
  type: http
  seq: 121
  tags:
    - Construction

http:
  method: GET
  url: "{{cmsUrl}}/api/cms/Construction/ReportServiceItemEntry"
  params:
    - name: cmLedgerItemId
      value: "942936210"
      type: query
      disabled: true
  auth:
    type: bearer
    token: "{{bearerToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/cms/Construction/ReportServiceItemEntry"
      method: GET
      params:
        - name: cmLedgerItemId
          value: ""
          type: query
          disabled: true
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
