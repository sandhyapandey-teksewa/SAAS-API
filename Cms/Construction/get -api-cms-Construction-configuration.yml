info:
  name: get /api/cms/Construction/configuration
  type: http
  seq: 1
  tags:
    - Construction

http:
  method: GET
  url: "{{cmsUrl}}/api/cms/Construction/configuration"
  auth:
    type: bearer
    token: "{{bearerToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/cms/Construction/configuration"
      method: GET
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
