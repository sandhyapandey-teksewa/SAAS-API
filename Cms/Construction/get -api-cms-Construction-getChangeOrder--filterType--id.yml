info:
  name: get /api/cms/Construction/getChangeOrder/:filterType/:id
  type: http
  seq: 54
  tags:
    - Construction

http:
  method: GET
  url: "{{cmsUrl}}/api/cms/Construction/getChangeOrder/:filterType/:id"
  params:
    - name: filterType
      value: ""
      type: path
    - name: id
      value: "3727570"
      type: path
  auth:
    type: bearer
    token: "{{bearerToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/cms/Construction/getChangeOrder/:filterType/:id"
      method: GET
      params:
        - name: filterType
          value: ""
          type: path
        - name: id
          value: ""
          type: path
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
