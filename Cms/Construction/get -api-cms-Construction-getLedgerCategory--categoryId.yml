info:
  name: get /api/cms/Construction/getLedgerCategory/:categoryId
  type: http
  seq: 8
  tags:
    - Construction

http:
  method: GET
  url: "{{cmsUrl}}/api/cms/Construction/getLedgerCategory/:categoryId"
  params:
    - name: categoryId
      value: "937767097"
      type: path
  auth:
    type: bearer
    token: "{{bearerToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/cms/Construction/getLedgerCategory/:categoryId"
      method: GET
      params:
        - name: categoryId
          value: ""
          type: path
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
