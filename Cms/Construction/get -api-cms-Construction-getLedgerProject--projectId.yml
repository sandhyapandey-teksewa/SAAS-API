info:
  name: get /api/cms/Construction/getLedgerProject/:projectId
  type: http
  seq: 6
  tags:
    - Construction

http:
  method: GET
  url: "{{cmsUrl}}/api/cms/Construction/getLedgerProject/:projectId"
  params:
    - name: projectId
      value: "937761010"
      type: path
  auth:
    type: bearer
    token: "{{bearerToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/cms/Construction/getLedgerProject/:projectId"
      method: GET
      params:
        - name: projectId
          value: ""
          type: path
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
