info:
  name: get /api/cms/Construction/getUnsignedDateDropdown/:contractId
  type: http
  seq: 78
  tags:
    - Construction

http:
  method: GET
  url: "{{cmsUrl}}/api/cms/Construction/getUnsignedDateDropdown/:contractId"
  params:
    - name: contractId
      value: "3550203"
      type: path
  auth:
    type: bearer
    token: "{{bearerToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/cms/Construction/getUnsignedDateDropdown/:contractId"
      method: GET
      params:
        - name: contractId
          value: ""
          type: path
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
