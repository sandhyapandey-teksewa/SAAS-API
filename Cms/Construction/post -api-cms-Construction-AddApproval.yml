info:
  name: post /api/cms/Construction/AddApproval
  type: http
  seq: 22
  tags:
    - Construction

http:
  method: POST
  url: "{{cmsUrl}}/api/cms/Construction/AddApproval"
  body:
    type: json
    data: |-
      {
        "cmPayEstimateId": 941195910,
        "selectedItems": []
      }
  auth:
    type: bearer
    token: "{{bearerToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/cms/Construction/AddApproval"
      method: POST
      body:
        type: json
        data: |-
          {
            "cmPayEstimateId": 0,
            "selectedItems": []
          }
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
