info:
  name: post /api/cms/Construction/AddChangeOrderCategoryItems
  type: http
  seq: 65
  tags:
    - Construction

http:
  method: POST
  url: "{{cmsUrl}}/api/cms/Construction/AddChangeOrderCategoryItems"
  body:
    type: json
    data: |-
      {
        "categoryId": 0,
        "ledgerItemIds": []
      }
  auth:
    type: bearer
    token: "{{bearerToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/cms/Construction/AddChangeOrderCategoryItems"
      method: POST
      body:
        type: json
        data: |-
          {
            "categoryId": 0,
            "ledgerItemIds": []
          }
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
