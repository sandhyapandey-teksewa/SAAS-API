info:
  name: post /api/cms/Construction/AddNewChangeOrder/:projectId
  type: http
  seq: 99
  tags:
    - Construction

http:
  method: POST
  url: "{{cmsUrl}}/api/cms/Construction/AddNewChangeOrder/:projectId"
  params:
    - name: projectId
      value: "3184920"
      type: path
  auth:
    type: bearer
    token: "{{bearerToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/cms/Construction/AddNewChangeOrder/:projectId"
      method: POST
      params:
        - name: projectId
          value: ""
          type: path
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
