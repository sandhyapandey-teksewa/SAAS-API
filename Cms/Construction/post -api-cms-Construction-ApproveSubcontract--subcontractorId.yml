info:
  name: post /api/cms/Construction/ApproveSubcontract/:subcontractorId
  type: http
  seq: 44
  tags:
    - Construction

http:
  method: POST
  url: "{{cmsUrl}}/api/cms/Construction/ApproveSubcontract/:subcontractorId"
  params:
    - name: subcontractorId
      value: "0"
      type: path
  auth:
    type: bearer
    token: "{{bearerToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/cms/Construction/ApproveSubcontract/:subcontractorId"
      method: POST
      params:
        - name: subcontractorId
          value: ""
          type: path
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
