info:
  name: post /api/cms/Construction/ChangeOrderComplete
  type: http
  seq: 98
  tags:
    - Construction

http:
  method: POST
  url: "{{cmsUrl}}/api/cms/Construction/ChangeOrderComplete"
  params:
    - name: isCompletedIndChecked
      value: "false"
      type: query
      disabled: true
    - name: changeOrderId
      value: "5478827"
      type: query
      disabled: true
  auth:
    type: bearer
    token: "{{bearerToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/cms/Construction/ChangeOrderComplete"
      method: POST
      params:
        - name: isCompletedIndChecked
          value: ""
          type: query
          disabled: true
        - name: changeOrderId
          value: ""
          type: query
          disabled: true
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
