info:
  name: post /api/cms/Construction/CloseContract
  type: http
  seq: 111
  tags:
    - Construction

http:
  method: POST
  url: "{{baseUrl}}/api/cms/Construction/CloseContract"
  body:
    type: json
    data: "{}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/cms/Construction/CloseContract"
      method: POST
      body:
        type: json
        data: "{}"
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
