info:
  name: post /api/cms/Construction/DeleteChangeOrder/:changeOrderId
  type: http
  seq: 57
  tags:
    - Construction

http:
  method: POST
  url: "{{cmsUrl}}/api/cms/Construction/DeleteChangeOrder/:changeOrderId"
  params:
    - name: changeOrderId
      value: "942937410"
      type: path
  auth:
    type: bearer
    token: "{{bearerToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/cms/Construction/DeleteChangeOrder/:changeOrderId"
      method: POST
      params:
        - name: changeOrderId
          value: ""
          type: path
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
