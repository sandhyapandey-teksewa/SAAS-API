info:
  name: post /api/cms/Construction/GetChangeOrderApproval
  type: http
  seq: 58
  tags:
    - Construction

http:
  method: POST
  url: "{{cmsUrl}}/api/cms/Construction/GetChangeOrderApproval"
  body:
    type: json
    data: |-
      {
        "ids": []
      }
  auth:
    type: bearer
    token: "{{bearerToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/cms/Construction/GetChangeOrderApproval"
      method: POST
      body:
        type: json
        data: |-
          {
            "ids": []
          }
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
