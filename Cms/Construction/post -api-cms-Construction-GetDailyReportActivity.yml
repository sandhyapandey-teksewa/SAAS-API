info:
  name: post /api/cms/Construction/GetDailyReportActivity
  type: http
  seq: 116
  tags:
    - Construction

http:
  method: POST
  url: "{{cmsUrl}}/api/cms/Construction/GetDailyReportActivity"
  params:
    - name: contractId
      value: ""
      type: query
      disabled: true
  body:
    type: json
    data: |-
      [
        {}
      ]
  auth:
    type: bearer
    token: "{{bearerToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/cms/Construction/GetDailyReportActivity"
      method: POST
      params:
        - name: contractId
          value: ""
          type: query
          disabled: true
      body:
        type: json
        data: |-
          [
            {}
          ]
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
