info:
  name: post /api/cms/Construction/GetLedgerItem
  type: http
  seq: 67
  tags:
    - Construction

http:
  method: POST
  url: "{{cmsUrl}}/api/cms/Construction/GetLedgerItem"
  body:
    type: json
    data: |-
      {
        "smStandardItemId":1558349,
        "ebContractId": 3550203
      }
  auth:
    type: bearer
    token: "{{bearerToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/cms/Construction/GetLedgerItem"
      method: POST
      body:
        type: json
        data: |-
          {
            "smStandardItemId": 0,
            "ebContractId": 0
          }
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
