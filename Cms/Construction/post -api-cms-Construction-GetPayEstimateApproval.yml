info:
  name: post /api/cms/Construction/GetPayEstimateApproval
  type: http
  seq: 15
  tags:
    - Construction

http:
  method: POST
  url: "{{cmsUrl}}/api/cms/Construction/GetPayEstimateApproval"
  body:
    type: json
    data: |-
      {
        "approvalTypeCodes": []
      }
  auth:
    type: bearer
    token: "{{bearerToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/cms/Construction/GetPayEstimateApproval"
      method: POST
      body:
        type: json
        data: |-
          {
            "approvalTypeCodes": []
          }
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
