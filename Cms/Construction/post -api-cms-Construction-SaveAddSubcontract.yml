info:
  name: post /api/cms/Construction/SaveAddSubcontract
  type: http
  seq: 31
  tags:
    - Construction

http:
  method: POST
  url: "{{cmsUrl}}/api/cms/Construction/SaveAddSubcontract"
  body:
    type: json
    data: |-
      {
        "companyId": "2",
        "description": "Add",
        "contractId": "3214161"
      }
  auth:
    type: bearer
    token: "{{bearerToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/cms/Construction/SaveAddSubcontract"
      method: POST
      body:
        type: json
        data: |-
          {
            "companyId": 0,
            "description": "",
            "contractId": 0
          }
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
