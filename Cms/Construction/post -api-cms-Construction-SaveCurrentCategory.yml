info:
  name: post /api/cms/Construction/SaveCurrentCategory
  type: http
  seq: 101
  tags:
    - Construction

http:
  method: POST
  url: "{{cmsUrl}}/api/cms/Construction/SaveCurrentCategory"
  body:
    type: json
    data: |-
      {
        "cmChangeOrderId": 5473947,
        "list": [
          {
            "name": "PIKE INSDUSTRIES INC",
            "value": "ARCH"
          }
        ]
      }
  auth:
    type: bearer
    token: "{{bearerToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/cms/Construction/SaveCurrentCategory"
      method: POST
      body:
        type: json
        data: |-
          {
            "cmChangeOrderId": 0,
            "list": [
              {
                "name": "",
                "value": 0
              }
            ]
          }
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
