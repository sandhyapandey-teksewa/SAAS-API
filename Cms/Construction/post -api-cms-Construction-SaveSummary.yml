info:
  name: post /api/cms/Construction/SaveSummary
  type: http
  seq: 11
  tags:
    - Construction

http:
  method: POST
  url: "{{cmsUrl}}/api/cms/Construction/SaveSummary"
  body:
    type: json
    data: |-
      {
        "id": 937760910,
        "fhwaOversight": true,
        "originalCompletionDate": "06-04-2025"
      }
  auth:
    type: bearer
    token: "{{bearerToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/cms/Construction/SaveSummary"
      method: POST
      body:
        type: json
        data: |-
          {
            "id": 0,
            "fhwaOversight": false,
            "originalCompletionDate": ""
          }
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
