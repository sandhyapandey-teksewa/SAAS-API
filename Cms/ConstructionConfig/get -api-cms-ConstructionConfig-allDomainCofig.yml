info:
  name: get /api/cms/ConstructionConfig/allDomainCofig
  type: http
  seq: 1
  tags:
    - ConstructionConfig

http:
  method: GET
  url: "{{cmsUrl}}/api/cms/ConstructionConfig/allDomainCofig"
  auth:
    type: bearer
    token: "{{bearerToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/cms/ConstructionConfig/allDomainCofig"
      method: GET
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
