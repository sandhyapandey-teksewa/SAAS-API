info:
  name: get /api/cms/ConstructionConfig/success
  type: http
  seq: 3
  tags:
    - ConstructionConfig

http:
  method: GET
  url: "{{cmsUrl}}/api/cms/ConstructionConfig/success"
  params:
    - name: message
      value: ""
      type: query
      disabled: true
  auth:
    type: bearer
    token: "{{bearerToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/cms/ConstructionConfig/success"
      method: GET
      params:
        - name: message
          value: ""
          type: query
          disabled: true
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
