info:
  name: get /api/cms/Document/DownloadZipArchive
  type: http
  seq: 5
  tags:
    - Document

http:
  method: GET
  url: "{{cmsUrl}}/api/cms/Document/DownloadZipArchive?ids=218382610408405"
  params:
    - name: ids
      value: "218382610408405"
      type: query
  auth:
    type: bearer
    token: "{{bearerToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/cms/Document/DownloadZipArchive"
      method: GET
      params:
        - name: ids
          value: ""
          type: query
          disabled: true
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
