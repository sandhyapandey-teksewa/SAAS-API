info:
  name: get /api/cms/Document/GetDocumentLiraryRecords
  type: http
  seq: 3
  tags:
    - Document

http:
  method: GET
  url: "{{cmsUrl}}/api/cms/Document/GetDocumentLiraryRecords"
  auth:
    type: bearer
    token: "{{bearerToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/cms/Document/GetDocumentLiraryRecords"
      method: GET
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
