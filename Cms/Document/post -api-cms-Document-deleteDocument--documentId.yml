info:
  name: post /api/cms/Document/deleteDocument/:documentId
  type: http
  seq: 7
  tags:
    - Document

http:
  method: POST
  url: "{{baseUrl}}/api/cms/Document/deleteDocument/:documentId"
  params:
    - name: documentId
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/cms/Document/deleteDocument/:documentId"
      method: POST
      params:
        - name: documentId
          value: ""
          type: path
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
