info:
  name: post /api/cms/Document/download
  type: http
  seq: 1
  tags:
    - Document

http:
  method: POST
  url: "{{cmsUrl}}/api/cms/Document/download"
  body:
    type: json
    data: "{}"
  auth:
    type: bearer
    token: "{{bearerToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/cms/Document/download"
      method: POST
      body:
        type: json
        data: "{}"
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: text/plain
      body:
        type: text
        data: ""
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/cms/Document/download"
      method: POST
      body:
        type: json
        data: "{}"
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: ""
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/cms/Document/download"
      method: POST
      body:
        type: json
        data: "{}"
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: text/json
      body:
        type: json
        data: ""
