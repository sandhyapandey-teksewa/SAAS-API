info:
  name: delete /api/cms/FormBuilder/DeleteSubmission/:submissionId
  type: http
  seq: 3
  tags:
    - FormBuilder

http:
  method: DELETE
  url: "{{cmsUrl}}/api/cms/FormBuilder/DeleteSubmission/:submissionId"
  params:
    - name: submissionId
      value: ""
      type: path
  auth:
    type: bearer
    token: "{{bearerToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/cms/FormBuilder/DeleteSubmission/:submissionId"
      method: DELETE
      params:
        - name: submissionId
          value: ""
          type: path
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
