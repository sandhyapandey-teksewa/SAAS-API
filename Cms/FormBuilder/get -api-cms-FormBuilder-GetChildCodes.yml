info:
  name: get /api/cms/FormBuilder/GetChildCodes
  type: http
  seq: 15
  tags:
    - FormBuilder

http:
  method: GET
  url: "{{cmsUrl}}/api/cms/FormBuilder/GetChildCodes"
  params:
    - name: parentCodeValue
      value: ""
      type: query
      disabled: true
  auth:
    type: bearer
    token: "{{bearerToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/cms/FormBuilder/GetChildCodes"
      method: GET
      params:
        - name: parentCodeValue
          value: ""
          type: query
          disabled: true
        - name: ignoreDate
          value: "false"
          type: query
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
