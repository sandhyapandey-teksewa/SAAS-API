info:
  name: get /api/cms/FormBuilder/GetChildCodesByCodeTypeCd
  type: http
  seq: 16
  tags:
    - FormBuilder

http:
  method: GET
  url: "{{cmsUrl}}/api/cms/FormBuilder/GetChildCodesByCodeTypeCd"
  params:
    - name: codeTypeCd
      value: ""
      type: query
      disabled: true
  auth:
    type: bearer
    token: "{{bearerToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/cms/FormBuilder/GetChildCodesByCodeTypeCd"
      method: GET
      params:
        - name: codeTypeCd
          value: ""
          type: query
          disabled: true
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
