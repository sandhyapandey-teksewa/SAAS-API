info:
  name: get /api/cms/FormBuilder/GetFormConfiguration/:id
  type: http
  seq: 5
  tags:
    - FormBuilder

http:
  method: GET
  url: "{{cmsUrl}}/api/cms/FormBuilder/GetFormConfiguration/:id"
  params:
    - name: id
      value: "4196416"
      type: path
  auth:
    type: bearer
    token: "{{bearerToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/cms/FormBuilder/GetFormConfiguration/:id"
      method: GET
      params:
        - name: id
          value: ""
          type: path
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
