info:
  name: get /api/cms/FormBuilder/GetParentCodes
  type: http
  seq: 14
  tags:
    - FormBuilder

http:
  method: GET
  url: "{{cmsUrl}}/api/cms/FormBuilder/GetParentCodes"
  auth:
    type: bearer
    token: "{{bearerToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/cms/FormBuilder/GetParentCodes"
      method: GET
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
