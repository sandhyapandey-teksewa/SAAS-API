info:
  name: get /api/cms/FormBuilder/success
  type: http
  seq: 18
  tags:
    - FormBuilder

http:
  method: GET
  url: "{{cmsUrl}}/api/cms/FormBuilder/success"
  params:
    - name: message
      value: ""
      type: query
      disabled: true
  auth:
    type: bearer
    token: "{{bearerToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/cms/FormBuilder/success"
      method: GET
      params:
        - name: message
          value: ""
          type: query
          disabled: true
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
