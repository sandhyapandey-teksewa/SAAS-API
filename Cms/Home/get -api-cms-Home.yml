info:
  name: get /api/cms/Home
  type: http
  seq: 1
  tags:
    - Home

http:
  method: GET
  url: "{{baseUrl}}/api/cms/Home"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/cms/Home"
      method: GET
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
