info:
  name: get /api/cms/Ledger/Categories/:projectId
  type: http
  seq: 1
  tags:
    - Ledger

http:
  method: GET
  url: "{{cmsUrl}}/api/cms/Ledger/Categories/:projectId"
  params:
    - name: projectId
      value: "942932410"
      type: path
  auth:
    type: bearer
    token: "{{bearerToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/cms/Ledger/Categories/:projectId"
      method: GET
      params:
        - name: projectId
          value: ""
          type: path
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
