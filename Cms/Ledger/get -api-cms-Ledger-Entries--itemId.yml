info:
  name: get /api/cms/Ledger/Entries/:itemId
  type: http
  seq: 2
  tags:
    - Ledger

http:
  method: GET
  url: "{{cmsUrl}}/api/cms/Ledger/Entries/:itemId"
  params:
    - name: itemId
      value: "942936410"
      type: path
  auth:
    type: bearer
    token: "{{bearerToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/cms/Ledger/Entries/:itemId"
      method: GET
      params:
        - name: itemId
          value: ""
          type: path
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
