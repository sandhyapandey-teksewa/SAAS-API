info:
  name: get /api/cms/Ledger/EntryTypes/:ledgerItemId
  type: http
  seq: 4
  tags:
    - Ledger

http:
  method: GET
  url: "{{baseUrl}}/api/cms/Ledger/EntryTypes/:ledgerItemId"
  params:
    - name: ledgerItemId
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/cms/Ledger/EntryTypes/:ledgerItemId"
      method: GET
      params:
        - name: ledgerItemId
          value: ""
          type: path
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
