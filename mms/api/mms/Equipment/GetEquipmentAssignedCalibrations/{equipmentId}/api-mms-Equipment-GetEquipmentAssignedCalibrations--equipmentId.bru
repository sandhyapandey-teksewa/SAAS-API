meta {
  name: /api/mms/Equipment/GetEquipmentAssignedCalibrations/:equipmentId
  type: http
  seq: 1
}

get {
  url: {{mmsUrl}}/api/mms/Equipment/GetEquipmentAssignedCalibrations/:equipmentId
  body: none
  auth: bearer
}

params:path {
  equipmentId: 939388510
}

auth:bearer {
  token: {{bearerToken}}
}

settings {
  encodeUrl: true
  timeout: 0
}

example {
  name: OK
  
  request: {
    url: {{baseUrl}}/api/mms/Equipment/GetEquipmentAssignedCalibrations/:equipmentId
    method: GET
    mode: none
    params:path: {
      equipmentId: 698
    }
  
    headers: {
      Authorization: Bearer <token>
    }
  }
  
  response: {
    status: {
      code: OK
      text: 200
    }
  
    body: {
      type: text
      content: '''
  
      '''
    }
  }
}
