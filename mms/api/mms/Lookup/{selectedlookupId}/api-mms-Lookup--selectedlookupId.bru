meta {
  name: /api/mms/Lookup/:selectedlookupId
  type: http
  seq: 1
}

get {
  url: {{mmsUrl}}/api/mms/Lookup/:selectedlookupId
  body: none
  auth: bearer
}

params:path {
  selectedlookupId: 3723401
}

auth:bearer {
  token: {{bearerToken}}
}

settings {
  encodeUrl: true
  timeout: 0
}

example {
  name: OK
  
  request: {
    url: {{baseUrl}}/api/mms/Lookup/:selectedlookupId
    method: GET
    mode: none
    params:path: {
      selectedlookupId: 698
    }
  
    headers: {
      Authorization: Bearer <token>
    }
  }
  
  response: {
    status: {
      code: OK
      text: 200
    }
  
    body: {
      type: text
      content: '''
  
      '''
    }
  }
}
