meta {
  name: /api/mms/Materials/GetSurveryForDropdown
  type: http
  seq: 1
}

get {
  url: {{mmsUrl}}/api/mms/Materials/GetSurveryForDropdown?searchText=
  body: none
  auth: bearer
}

params:query {
  searchText: 
}

auth:bearer {
  token: {{bearerToken}}
}

settings {
  encodeUrl: true
  timeout: 0
}

example {
  name: OK
  
  request: {
    url: {{baseUrl}}/api/mms/Materials/GetSurveryForDropdown?searchText=
    method: GET
    mode: none
    params:query: {
      searchText: 
    }
  
    headers: {
      Authorization: Bearer <token>
    }
  }
  
  response: {
    status: {
      code: OK
      text: 200
    }
  
    body: {
      type: text
      content: '''
  
      '''
    }
  }
}
