meta {
  name: /api/mms/Sample/GetAllSampleDoropdownlist/:DropdownType
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/mms/Sample/GetAllSampleDoropdownlist/:DropdownType?searchText=
  body: none
  auth: inherit
}

params:query {
  searchText: 
}

params:path {
  DropdownType: string
}

settings {
  encodeUrl: true
}

example {
  name: OK
  
  request: {
    url: {{baseUrl}}/api/mms/Sample/GetAllSampleDoropdownlist/:DropdownType?searchText=
    method: GET
    mode: none
    params:query: {
      searchText: 
    }
  
    params:path: {
      DropdownType: string
    }
  
    headers: {
      Authorization: Bearer <token>
    }
  }
  
  response: {
    status: {
      code: OK
      text: 200
    }
  
    body: {
      type: text
      content: '''
  
      '''
    }
  }
}
